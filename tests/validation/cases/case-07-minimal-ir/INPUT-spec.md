# Case 07 — Minimal IR

**Input.** `input.yaml` — 3-slide IR, all required deck/slide fields present, all optional fields omitted.

**Tests.** Renderer robustness on slim input.

**Pipeline.** IR schema validation → Renderer (against the synthetic-template fixture).
